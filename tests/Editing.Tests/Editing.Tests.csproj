<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\TrestleBoard.Editing\TrestleBoard.Editing.csproj" />
    <!-- Editing itself never references Widgets (it sees them through IWidgetCatalog); the tests
         do, so WidgetController is exercised against the real registry rather than a mock. -->
    <ProjectReference Include="..\..\src\TrestleBoard.Widgets\TrestleBoard.Widgets.csproj" />
  </ItemGroup>
</Project>
